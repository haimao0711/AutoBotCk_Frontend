

services:
  stock-cms-client:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stock-cms-client
    ports:
      - '5300:3000'
    dns:
      - 8.8.8.8
    env_file:
      - .env.production
