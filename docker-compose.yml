
services:
  stock-frontend:
    image: "${DOCKER_IMAGE_NAME}:${DOCKER_IMAGE_TAG}"
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:3000"
    env_file:
      - .env.production
